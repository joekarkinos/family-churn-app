# Supabase — get from supabase.com → project → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...your_anon_key...

# Only used in Edge Functions + skrypty/server-side (nigdy nie eksponować w kliencie)
SUPABASE_SERVICE_ROLE_KEY=eyJ...your_service_role_key...

# Sekret do wyprowadzania ukrytych haseł Supabase Auth (model "PIN bramkuje Auth").
# Długi losowy ciąg, np. `openssl rand -hex 32`. Ten sam dla seedu i logowania.
AUTH_DERIVE_SECRET=zmien_mnie_na_dlugi_losowy_sekret

# Firebase Cloud Messaging — for push notifications (Android)
# Get from Firebase Console → Project Settings → Cloud Messaging
NEXT_PUBLIC_FCM_VAPID_KEY=B...your_vapid_key...
FCM_SERVER_KEY=AAAA...your_server_key...
